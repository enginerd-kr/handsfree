# grove worktree setup — https://github.com/enginerd-kr/grove
[setup]
copy = ["handsfree.config.json"]   # gitignored local config, taken from main's worktree
run  = ["pnpm install", "pnpm build"]
open = 'open -a "Visual Studio Code" .'
